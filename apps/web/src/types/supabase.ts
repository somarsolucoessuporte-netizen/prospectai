/**
 * Tipos gerados a partir do schema do banco (Supabase CLI).
 *
 * Gere com: npx supabase gen types typescript --project-id SEU_PROJETO > src/types/supabase.ts
 * Mantido como placeholder ate a primeira geracao real, na Fase 1.
 */
export type Database = Record<string, never>;
